{
    "arguments": [ "branch", "arch_host", "arch_qemu_dir", "arch_git_remote" ],
    "machine": "$arch_host",
    "workdir": "$arch_qemu_dir",
    "execute": [ "git fetch $arch_git_remote",
                 "git checkout $arch_git_remote/$branch" ]
}
