{
    "arguments": [ "arch_host", "arch_clang_m64_dir" ],
    "machine": "$arch_host",
    "depends": [ "../branch" ],
    "workdir": "$arch_clang_m64_dir",
    "execute": [ "rm -rf -- * || true" ]
}
