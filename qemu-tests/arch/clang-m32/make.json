{
    "arguments": [ "arch_host", "arch_clang_m32_dir" ],
    "machine": "$arch_host",
    "variable_jobs": true,
    "depends": [ "./clean" ],
    "workdir": "$arch_clang_m32_dir",
    "execute": [ "PKG_CONFIG_LIBDIR=/usr/lib32/pkgconfig make -j $__jobs" ],
    "threads": "$__jobs"
}
