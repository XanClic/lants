{
    "arguments": [ "arch_host", "arch_clang_m32_dir" ],
    "machine": "$arch_host",
    "depends": [ "../branch" ],
    "workdir": "$arch_clang_m32_dir",
    "execute": [ "rm -rf -- * || true" ]
}
